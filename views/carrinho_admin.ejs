<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Admin - Carrinhos dos Usuários</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet"> <!-- Fonte bonita -->
    <style>
        body {
            font-family: 'Roboto', sans-serif; /* Aplicando a fonte */
            background: linear-gradient(180deg, #00aaff 0%, #005577 100%); /* Gradiente de fundo */
            overflow: hidden; /* Oculta a barra de rolagem */
            font-size: 1.5rem;
            margin: 0;
            padding: 0;
        }

        .wave {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            pointer-events: none;
            animation: wave 10s infinite linear;
            z-index: -1;
        }

        @keyframes wave {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-20%);
            }
            100% {
                transform: translateY(0);
            }
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            color: white;
            text-align: center;
            padding: 20px;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        table {
            width: 40%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: #ffffff33; /* Leve fundo transparente */
            border-radius: 8px;
        }

        th, td {
            padding: 12px;
            text-align: center;
            border: 1px solid #005577;
        }

        th {
            background-color: #005577;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #e0f7fa;
        }

        tr:nth-child(odd) {
            background-color: #b2ebf2;
        }

        td {
            color: #333;
        }

        .button-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 10px;
        }

        .button {
            font-family: 'Roboto', sans-serif;
            font-size: large;
            background: #084692;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 20px;
            margin: 10px;
            width: 150px;
            text-align: center;
            font-size: 1.2rem;
            cursor: pointer;
            transition: background 0.3s;
            text-decoration: none;
        }

        .button:hover {
            background: #005bb5;
        }

        .user-info {
            margin-bottom: 20px;
            font-size: 1.2rem;
            color: #f0f0f0;
        }
    </style>
</head>
<body>
    <div class="wave"></div> <!-- Camada de efeito -->
    <div class="container">
        <h1>Carrinhos dos Usuários</h1>
        
        <% for (let username in carrinhosAgrupados) { %>
            <div class="user-info">
                <h2><%= username %></h2>
                <p><strong>Telefone:</strong> <%= carrinhosAgrupados[username].telefone %></p> <!-- Exibindo o telefone -->
            </div>
            
            <table>
                <tr>
                    <th>Item</th>
                    <th>Quantidade</th>
                </tr>
                <% carrinhosAgrupados[username].forEach(item => { %>
                    <tr>
                        <td><%= item.item_nome %></td>
                        <td><%= item.quantidade %></td>
                    </tr>
                <% }); %>
            </table>
        <% } %>

        <!-- Adicionando botões -->
        <div class="button-container">
            <a href="/cadastro" class="button">Cadastro de Usuário</a>
            <a href="/cadastro-item" class="button">Cadastro de Item</a>
            <a href="/itens" class="button">Lista de Itens</a>
            <a href="/logout" class="button">Sair</a>
        </div>
    </div>
</body>
</html>
